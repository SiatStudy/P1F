import styled from "styled-components";

const CustomPopupLabel = styled.label`
    display: flex;
    align-items: flex-start;
    gap: 0.25rem;
    align-self: stretch;
    color: #3E54AC;
    font-family: Noto Sans, serif;
    font-size: 0.875rem;
    font-style: normal;
    font-weight: 500;
    line-height: 1rem;
`;

export default CustomPopupLabel;